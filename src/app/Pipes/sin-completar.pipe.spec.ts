import { SinCompletarPipe } from './sin-completar.pipe';

describe('SinCompletarPipe', () => {
  it('create an instance', () => {
    const pipe = new SinCompletarPipe();
    expect(pipe).toBeTruthy();
  });
});
