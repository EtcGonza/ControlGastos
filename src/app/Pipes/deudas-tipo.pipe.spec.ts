import { DeudasTipoPipe } from './deudas-tipo.pipe';

describe('DeudasTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new DeudasTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
