import { IconsCategoriaPipe } from './icons-categoria.pipe';

describe('IconsCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new IconsCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
